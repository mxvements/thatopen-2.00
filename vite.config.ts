import { defineConfig } from 'vite';

export default defineConfig({
  root: 'course', // Set the root directory for your project
  build: {
    outDir: '../dist', // Output directory for production builds
  },
});